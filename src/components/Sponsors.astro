---
interface Props {
	title?: string
}

const { title = 'Sponsors' } = Astro.props

const sponsors = [
	{
		name: 'Trek Bikes',
		logo: '🚴',
		color: '#FF5900',
		url: '/logos/trek-logo.svg',
	},
	{
		name: 'Shimano',
		logo: '⚙️',
		color: '#1F4E78',
		url: '/logos/shimano-logo.webp',
	},
	{
		name: 'GoPro',
		logo: '📹',
		color: '#19A0C8',
		url: '/logos/gopro-logo.svg',
	},
	{
		name: 'Red Bull',
		logo: '🐂',
		color: '#001D3D',
		url: '/logos/redbull-logo.webp',
	},
	{
		name: 'Garmin',
		logo: '📍',
		color: '#004B87',
		url: '/logos/garmin-logo.svg',
	},
	{
		name: 'Canyon',
		logo: '🏔️',
		color: '#000000',
		url: '/logos/canyon-logo.webp',
	},
	{
		name: 'Wahoo',
		logo: '📊',
		color: '#2E8B57',
		url: '/logos/wahoo-logo.webp',
	},
	{
		name: 'PowerBar',
		logo: '⚡',
		color: '#FFB81C',
		url: '/logos/powerbar-logo.svg',
	},
]
---

<section class="sponsors-section">
	<h2 class="uppercase font-bold mb-8 text-center text-teal-600">
		{title}
	</h2>
	<div class="sponsors-container p-4">
		<div class="sponsors-track">
			{
				sponsors.map((sponsor) => (
					<div class="sponsor-item">
						<div class="sponsor-card bg-gray-700/10 dark:bg-gray-400/10">
							<div class="sponsor-logo">
								{sponsor.url ? (
									<img
										src={sponsor.url}
										alt={sponsor.name}
										style="height: 2rem; width: auto;"
									/>
								) : (
									<span style={`color: ${sponsor.color}`}>{sponsor.logo}</span>
								)}
							</div>
						</div>
					</div>
				))
			}
			{
				sponsors.map((sponsor) => (
					<div class="sponsor-item">
						<div class="sponsor-card bg-gray-700/10 dark:bg-gray-400/10">
							<div class="sponsor-logo">
								{sponsor.url ? (
									<img
										src={sponsor.url}
										alt={sponsor.name}
										style="height: 2rem; width: auto;"
									/>
								) : (
									<span style={`color: ${sponsor.color}`}>{sponsor.logo}</span>
								)}
							</div>
						</div>
					</div>
				))
			}
		</div>
	</div>
</section>

<style>
	.sponsors-section {
		width: 100%;
		padding: 3rem 2rem;
		position: relative;
		overflow: hidden;
	}

	.sponsors-section::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		pointer-events: none;
	}

	.sponsors-container {
		overflow: hidden;
		mask-image: linear-gradient(
			to right,
			transparent 0%,
			black 8%,
			black 92%,
			transparent 100%
		);
		-webkit-mask-image: linear-gradient(
			to right,
			transparent 0%,
			black 8%,
			black 92%,
			transparent 100%
		);
	}

	.sponsors-track {
		display: flex;
		gap: 1rem;
		animation: scroll-sponsors 35s linear infinite;
		width: fit-content;
	}

	@keyframes scroll-sponsors {
		0% {
			transform: translateX(0);
		}
		100% {
			transform: translateX(-50%);
		}
	}

	.sponsors-container:hover .sponsors-track {
		animation-play-state: paused;
	}

	.sponsor-item {
		flex-shrink: 0;
		display: flex;
		align-items: center;
		justify-content: center;
		min-width: max-content;
	}

	.sponsor-card {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		padding: 0.875rem 1.5rem;
		border-radius: 0.5rem;
		backdrop-filter: blur(10px);
		-webkit-backdrop-filter: blur(10px);
		transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
		white-space: nowrap;
		position: relative;
		overflow: hidden;
	}

	.sponsor-card::before {
		content: '';
		position: absolute;
		inset: 0;
		background: linear-gradient(
			135deg,
			rgba(19, 127, 236, 0.2) 0%,
			transparent 100%
		);
		opacity: 0;
		transition: opacity 0.3s ease;
	}

	.sponsor-item:hover .sponsor-card {
		background: rgba(19, 127, 236, 0.15);
		transform: translateY(-6px) scale(1.05);
	}

	.sponsor-item:hover .sponsor-card::before {
		opacity: 1;
	}

	.sponsor-logo {
		font-size: 1.5rem;
		display: inline-block;
		position: relative;
		z-index: 1;
		transition: transform 0.5s ease;
	}

	.sponsor-item:hover .sponsor-logo {
		transform: scale(1.2) rotate(-5deg);
	}

	.sponsor-card::after {
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
	}

	/* Responsivo */
	@media (max-width: 768px) {
		.sponsors-section {
			padding: 2rem 1rem;
		}

		.sponsor-card {
			padding: 0.625rem 1rem;
			gap: 0.5rem;
		}

		.sponsor-logo {
			font-size: 1.25rem;
		}

		.sponsor-item:hover .sponsor-card {
			transform: translateY(-4px) scale(1.02);
		}
	}
</style>
